#!/bin/bash
#SBATCH --qos normal
#SBATCH --mail-user=adam.rubin@colorado.edu
#SBATCH --time=04:00:00
#SBATCH --ntasks 1

echo "starting to run"

# Job file submitted via "msub <filename>".  
# In this case, N tasks are launched by srun, each of which executes 
#  ./rescal.sh
# (N is set as --ntasks=N)

# This file will run N tasks, with PMI ranks 0 to N

#srun --nodes=1 --cpus-per-task=1 --ntasks=34 --wait=0 submit.sh
./run


